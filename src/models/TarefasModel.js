class TarefasModel {
    constructor(titulo, data, descricaco, status) {
            this.titulo = titulo,
            this.data = data,
            this.descricaco = descricaco,
            this.status = status;
    }
}

export default TarefasModel;
